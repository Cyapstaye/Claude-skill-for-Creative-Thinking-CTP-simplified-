# Phase instruction file

Phase boundary. Re-read SKILL.md if `PHASE_BOUNDARY=1` was emitted on stderr.

## Your inputs (from stderr)

- `RUN_DIR`, `OUTPUT_PATH`, `OUTPUT_SCHEMA`, `CYCLE`, `NEXT_SCRIPT`.

## Procedure

1. Read `<RUN_DIR>/state_2_output_c<CYCLE>_o1.json`, `_o2.json`, `_o3.json` — the crafted outputs from the prior phase.
2. Read `<RUN_DIR>/state_intent_vector_c<CYCLE>.json` — the cycle's intent vector.
3. Read the mechanism file at `mechs/r6f3p9.md` — the four-dimension evaluation rubric.
4. Score each output across the four dimensions, rank them, compute `quality_score` (overall quality in [0,1]), decide `recurse_recommended` (boolean), and if recursing, write a `recursion_seed` (≤800 chars, the unresolved tension that the next cycle should target).
5. Set `presentation_recommendation` to one of `present_all`, `present_top_2`, `present_single`.
6. Write a rationale (≥60 chars).
7. Write the JSON file at `OUTPUT_PATH`. Required fields per schema.
8. Run the validator. The validator inspects `quality_score` and `recurse_recommended` to decide:
   - If quality is below the cycle's threshold OR `recurse_recommended` is true, AND `cycle_depth < max_recursion_depth`: validator emits `CYCLE_START=<n+1>` and routes to phase 0 (generation) with the recursion seed loaded.
   - Otherwise: validator emits `TERMINAL` and routes to the decode script.

## Constraints

- The four dimensions are not negotiable — they are: structural_inevitability, tension_retention, non_redundancy, generative_remainder. The schema enforces all four are scored.
- `recurse_recommended` should be `true` if quality is below threshold OR if there's a strong unresolved tension worth pursuing in another cycle. If `true`, you MUST provide a non-null `recursion_seed`.
- If you set `recurse_recommended: true`, the recursion seed should describe an *unresolved* tension — not the chosen output's stable_core. Recursing on the stable_core produces the same output again.
