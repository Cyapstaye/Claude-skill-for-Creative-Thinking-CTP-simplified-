# Recovery instruction file (run stuck)

The validator routed you here because a deterministic Python step (firewall) failed, or a non-recoverable run-level inconsistency was detected.

## Your inputs (from stderr)

- `ERROR_CLASS=run_stuck`
- `REASON` — description of what went wrong
- `RUN_DIR`

## Procedure

1. Halt.
2. Read `<RUN_DIR>/state_0.json` for context.
3. Surface to the user:
   - `original_query`
   - Last 5 entries of `operation_log` (most recent at bottom)
   - The `REASON` from stderr
4. Do not retry. Run-level failures (typically firewall errors, missing prior-phase files, or filesystem issues) require human inspection.

## Why this happened (likely causes)

- The firewall couldn't aggregate state_1_group files (e.g., one or more were missing or malformed).
- A prior phase's state file was deleted between turns.
- Schema or _dispatch role mapping mismatch.

## What the user can try

- Re-invoke the skill with the same query.
- Inspect `<RUN_DIR>/_audit/` for firewall trace data.
