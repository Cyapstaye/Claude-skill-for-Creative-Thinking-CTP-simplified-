# Phase instruction file (deterministic Python — should not normally be reached)

If you are reading this file, the dispatcher should have run a deterministic Python step on your behalf and skipped past this phase. The validator handles the selection step automatically by invoking the firewall script.

If you reached here as a model turn, something has gone wrong. Halt and surface to the user. Do not attempt to perform the selection logic yourself; the firewall script is authoritative for the whitelist strip and the model is forbidden from approximating it.

If you are at PHASE_BOUNDARY and need to resume cleanly, run the validator with the current run dir; it will detect the inconsistency and route to recovery.
