# Phase instruction file

Phase boundary. Re-read SKILL.md if `PHASE_BOUNDARY=1` was emitted on stderr.

## Your inputs (from stderr)

- `RUN_DIR`, `OUTPUT_PATH`, `OUTPUT_SCHEMA`, `CYCLE`, `NEXT_SCRIPT`.
- No `NEXT_PROFILE` for this phase — single-archetype-neutral.

## Procedure

1. Read `<RUN_DIR>/state_0.json` — capture `original_query` and `session_id`.
   - **Do not** read the shard files, grouping files, profile configs, or any Stage-1 artifact. The schema for this phase's output (`compiled_from: "original_query"` is a `const`) enforces this; if you include any reference to Stage-1 reasoning, validation will fail.
2. Read the mechanism file at `mechs/w8q2k5.md` — the intent vector contract.
3. Compile a three-field intent vector:
   - **pull**: what the query draws toward (direction, not destination)
   - **away_from**: what the query implicitly rejects
   - **tension**: the productive contradiction between them
4. Write the JSON file at `OUTPUT_PATH`. Required fields per schema.
5. Run the validator.

## Firewall

The schema's `compiled_from: { "const": "original_query" }` is the firewall. Any output that reads from Stage-1 artifacts will produce a JSON with shapes that fail validation. Compile from the original query alone.
