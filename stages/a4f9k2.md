# Phase instruction file

You are at a phase boundary. The dispatcher has set up your environment via stderr key=value pairs. Re-read SKILL.md if PHASE_BOUNDARY=1 was emitted (it is on this turn).

## Your inputs (from stderr metadata)

- `RUN_DIR` — your working directory
- `NEXT_PROFILE` — your archetype name for this turn
- `PROFILE_CONFIG_PATH` — absolute path to your profile config (read this first; the `priming_preamble` is operative voice)
- `OUTPUT_PATH` — absolute path where you write your output JSON
- `OUTPUT_SCHEMA` — absolute path to the JSON Schema your output must satisfy
- `CYCLE` — current cycle index (0 for first pass)
- `NEXT_SCRIPT` — the validator script you run after writing your output

## Procedure

1. Read `<RUN_DIR>/state_0.json` and capture `original_query` and `session_id`.
   - If `recursion_seed_path` is non-null in state_0, also read that file. Its content is an unresolved tension from a prior cycle that should orient this cycle's generation.
2. Read `PROFILE_CONFIG_PATH`. Internalize the `priming_preamble`. You are now operating as that archetype.
3. Read the mechanism file at `mechs/t4k9p2.md` (path relative to `$SKILL_ROOT`). It is the shard grammar — the contract for what a shard is, what it isn't, and the count/density rules.
4. Generate 6–14 shards from inside your archetype's operative mode against `original_query` (and `recursion_seed` content if set).
5. Write the JSON file at `OUTPUT_PATH`. Schema is at `OUTPUT_SCHEMA`. Required fields: `schema_version: "1"`, `session_id`, `archetype` (your `NEXT_PROFILE`), `cycle` (your `CYCLE`), `shards` (the array).
6. Run the validator: `python3 <NEXT_SCRIPT> --run-dir <RUN_DIR> --last-output <OUTPUT_PATH>`. The validator's stdout will tell you the next file to read.

## Constraints

- Stay inside the operative mode. The priming_preamble is not a description of the archetype — it is the voice you write in.
- 20–160 chars per shard. The validator enforces this.
- 6–14 shards. The validator enforces this.
- Do not write to any path other than `OUTPUT_PATH`.
- Do not read any file not listed above.
