# Phase instruction file

Phase boundary. Re-read SKILL.md if `PHASE_BOUNDARY=1` was emitted on stderr.

## Your inputs (from stderr)

- `RUN_DIR`, `OUTPUT_PATH`, `OUTPUT_SCHEMA`, `CYCLE`, `NEXT_OUTPUT_ID`, `NEXT_SCRIPT`.
- No `NEXT_PROFILE` (this phase uses a unified voice across crafting iterations).

## Procedure

1. Read `<RUN_DIR>/state_1_crossed_c<CYCLE>.json` — the post-firewall shard groups (cleared whitelist).
2. Read `<RUN_DIR>/state_intent_vector_c<CYCLE>.json` — the direction-without-destination compiled from the original query.
3. Compose a single crafted output. The output_id is `NEXT_OUTPUT_ID` (1, 2, or 3); each output should take a different starting angle relative to the intent vector's `tension`.
4. The output has five iteration trace entries (each with `iter_index`, `mashed_configuration`, `observed_emergence`, `recombination_decision`) plus a `final_form` (≥120 chars) and `emergent_properties` (2–3 entries).
5. Write the JSON file at `OUTPUT_PATH`. Required fields per schema.
6. Run the validator.

## Constraints

- Each output (id 1, 2, 3) must take a *materially different* starting angle. If output 2 reads as a paraphrase of output 1, both fail the protocol's non-redundancy goal — the evaluator will catch this and may recommend recursion.
- The `iteration_trace` is genuine internal reasoning written down — five iterations, each transforming the prior one. Don't fake five identical entries.
- `final_form` honours the operative voice the protocol has built up. It is dense; no preamble, no meta-commentary.
