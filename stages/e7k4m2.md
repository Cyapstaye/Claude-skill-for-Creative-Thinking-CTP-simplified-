# Recovery instruction file (retry exhausted)

The validator routed you here because the same phase failed schema validation more times than the retry budget allows.

## Your inputs (from stderr)

- `ERROR_CLASS=retry_exhausted`
- `FAILED_PATH` — the state file path that repeatedly failed validation
- `REASON` — short description of the last validation error
- `RUN_DIR` — your working directory

## Procedure

1. Halt. Do not attempt another retry — the budget is exhausted by definition.
2. Read `<RUN_DIR>/state_0.json` to get the session context.
3. Surface to the user:
   - The `original_query`
   - The phase that failed (decoded from the operation_log's last entries)
   - The `REASON` from stderr
   - The `FAILED_PATH` — they may want to inspect it
   - Suggest: re-running the protocol from scratch, or inspecting the failed state file to see what the model produced.
4. Do not run any further script. The protocol has halted.

## Why this happened (likely causes)

- The mech's output requirements are unfulfillable given the input. (e.g., shard count requirement of 6–14 but the archetype produced 4 each time.)
- The schema is too strict for the specific input. (Edge case in the original_query.)
- The model is stuck in a bad-attractor and producing the same invalid output repeatedly.

## What the user can try

- Re-invoke the skill with the same query — the random run_hex will produce different profile noise, which may unblock.
- Re-invoke with a slightly rephrased query.
- Lower-priority: inspect the run dir at `<RUN_DIR>` and report findings.
