# Phase instruction file

Phase boundary. Re-read SKILL.md if `PHASE_BOUNDARY=1` was emitted on stderr.

## Your inputs (from stderr)

- `RUN_DIR`, `OUTPUT_PATH`, `OUTPUT_SCHEMA`, `CYCLE`, `NEXT_SCRIPT`.

## Procedure

1. Read `<RUN_DIR>/state_evaluation_c<CYCLE>.json` — the evaluator's verdict, including `presentation_recommendation`.
2. Read `<RUN_DIR>/state_2_output_c<CYCLE>_o1.json`, `_o2.json`, `_o3.json` — the crafted outputs.
3. Read `<RUN_DIR>/state_intent_vector_c<CYCLE>.json` — the cycle's intent vector, for tone reference.
4. Read the mechanism file at `mechs/h8w3p9.md` — the polish/formatting contract.
5. Filter outputs by the evaluator's `presentation_recommendation`. Render the surviving outputs as a clean markdown document. Preserve substance; improve presentation only.
6. Write the JSON file at `OUTPUT_PATH`. Required fields per schema. The `polished_markdown` field is a single JSON-encoded string containing the full markdown.
7. Run the validator: `python3 <NEXT_SCRIPT> --run-dir <RUN_DIR> --last-output <OUTPUT_PATH>`. The validator will then emit `TERMINAL` and route to decode, which writes your `polished_markdown` verbatim to `CTP_brief_<slug>.md`.

## Constraints

- Do not change substance. The crafting phase already chose the ideas and the words. You are formatting them.
- Do not add preamble, footer, or framing.
- Do not synthesize across outputs. Each output stays distinct in the polished document.
- 120+ chars in `polished_markdown` — schema enforces minimum length.
